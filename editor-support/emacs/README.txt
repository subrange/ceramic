Steps to install ceramic-mode in emacs

1) Copy ceramic-mode.el to a directory of your choice (E.g. ~/emacs.d/ceramic)

2) Add the following two pieces of lines to your .emacs file -
   (add-to-list 'load-path "~/.emacs.d/ceramic/")
   (require 'ceramic-mode)
